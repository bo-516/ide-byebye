/**
 * Mako (Umi) entry: `import inspector from 'ide-byebye/mako'`.
 * Path injection only; bootstrap still needs an HTML entry.
 */
export type { IdeByebyeOptions, PluginInstance } from '../types.js';
export { mako, mako as default } from '../plugin.js';
